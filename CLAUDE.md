# Claude Code Instructions

**At the start of every session, read `README.md` before doing anything else.** It contains the full architecture, database schema, auth model, account types, page inventory, and key file locations. Do not crawl the directory structure to re-derive context that is already there.

Also read `frontend/AGENTS.md` before writing any Next.js code — this version has breaking API changes from standard Next.js.
